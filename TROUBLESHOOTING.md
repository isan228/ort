# Решение проблем

## Ошибка: ERR_CONNECTION_REFUSED

### Проблема
```
Error loading free tests: AxiosError
Failed to load resource: net::ERR_CONNECTION_REFUSED
:5000/api/tests/free-by-subject:1
```

### Причины
1. **Сервер не запущен** - Node.js сервер не работает на порту 5000
2. **Неправильный URL в .env** - файл `.env` в клиенте не настроен или отсутствует
3. **Порт занят** - другой процесс использует порт 5000

### Решение

#### Шаг 1: Проверьте, запущен ли сервер

```bash
# В отдельном терминале/окне
cd server
npm start
```

Должно появиться:
```
Database connected successfully
🔄 Автоматическая синхронизация БД включена (alter: true)
Server is running on port 5000
```

#### Шаг 2: Проверьте файл .env в клиенте

```bash
cd client
ls -la .env  # Linux/Mac
dir .env     # Windows
```

Если файла `.env` нет, создайте его:

```bash
cd client
cp env.example .env
```

Отредактируйте `client/.env`:

```env
REACT_APP_API_URL=http://localhost:5000/api
```

#### Шаг 3: Перезапустите клиент

После изменения `.env` **ОБЯЗАТЕЛЬНО** перезапустите клиент:

```bash
# Остановите клиент (Ctrl+C)
# Затем запустите снова
npm start
```

**Важно:** React не перезагружает переменные окружения из `.env` во время работы. Нужен полный перезапуск!

#### Шаг 4: Проверьте, что оба сервера запущены

**Терминал 1 (Backend):**
```bash
cd server
npm start
```

**Терминал 2 (Frontend):**
```bash
cd client
npm start
```

#### Шаг 5: Используйте скрипт для одновременного запуска

```bash
# В корне проекта
npm run dev
```

Это запустит оба сервера одновременно.

### Проверка подключения

#### Проверка API в браузере

Откройте в браузере:
```
http://localhost:5000/api/health
```

Должен вернуться JSON:
```json
{"status":"OK","message":"Server is running"}
```

#### Проверка через curl

```bash
curl http://localhost:5000/api/health
```

### Дополнительные проверки

#### Проверка порта 5000

**Windows:**
```bash
netstat -ano | findstr :5000
```

**Linux/Mac:**
```bash
lsof -i :5000
# или
sudo ss -tulpn | grep :5000
```

Если порт занят, либо:
1. Остановите процесс, использующий порт
2. Измените порт в `server/.env`: `PORT=5001`

#### Проверка переменных окружения

В консоли браузера (F12 → Console):
```javascript
console.log(process.env.REACT_APP_API_URL);
```

Должно показать: `http://localhost:5000/api`

### Частые ошибки

#### Ошибка: "Cannot read property 'REACT_APP_API_URL' of undefined"

**Причина:** Переменные окружения не загружены.

**Решение:** 
1. Убедитесь, что файл называется `.env` (с точкой в начале!)
2. Перезапустите клиент после изменения `.env`
3. Переменные должны начинаться с `REACT_APP_`

#### Ошибка: "Network Error"

**Причина:** Сервер не запущен или недоступен.

**Решение:**
1. Запустите сервер: `cd server && npm start`
2. Проверьте, что порт 5000 свободен
3. Проверьте firewall/антивирус

#### Ошибка: "CORS policy"

**Причина:** Сервер не разрешает запросы с клиента.

**Решение:** Убедитесь, что в `server/index.js` включен CORS:
```javascript
app.use(cors());
```

### Быстрое решение

Если ничего не помогает:

```bash
# 1. Остановите все процессы
# Ctrl+C в обоих терминалах

# 2. Убедитесь, что .env файлы существуют
cd server
ls -la .env  # Должен быть файл
cd ../client
ls -la .env  # Должен быть файл

# 3. Используйте скрипт для одновременного запуска
cd ..
npm run dev
```

Или используйте скрипты запуска:
- Windows: `start.bat`
- Linux/Mac: `start.sh`
- PowerShell: `start.ps1`

