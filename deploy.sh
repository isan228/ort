#!/bin/bash

# Скрипт для автоматического развертывания ORT платформы
# Использование: ./deploy.sh

set -e  # Остановка при ошибке

echo "🚀 Начинаем развертывание ORT платформы..."

# Цвета для вывода
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
NC='\033[0m' # No Color

# Проверка, что мы в правильной директории
if [ ! -f "package.json" ]; then
    echo -e "${RED}❌ Ошибка: package.json не найден. Убедитесь, что вы в корне проекта.${NC}"
    exit 1
fi

echo -e "${YELLOW}📦 Обновление зависимостей...${NC}"
npm run install:all

echo -e "${YELLOW}🔨 Сборка клиентской части...${NC}"
cd client
npm run build
cd ..

echo -e "${YELLOW}🔄 Перезапуск сервера через PM2...${NC}"
if pm2 list | grep -q "ort-server"; then
    pm2 restart ort-server
else
    pm2 start ecosystem.config.js
    pm2 save
fi

echo -e "${GREEN}✅ Развертывание завершено!${NC}"
echo -e "${GREEN}📊 Статус:${NC}"
pm2 status

echo -e "${YELLOW}📝 Логи сервера:${NC}"
echo "Для просмотра логов используйте: pm2 logs ort-server"

